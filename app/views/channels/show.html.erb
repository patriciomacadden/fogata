<%= render 'subnav' %>

<div class="messages">
  <%= render @channel.messages %>
</div>

<%= render 'messages/form' %>

<%= subscribe_to channel_path(@channel) %>
<%= subscribe_to change_current_topic_channel_path(@channel) %>
<%= subscribe_to im_online_channel_path(@channel) %>
<script>
  imOnline('<%= im_online_channel_path(@channel) %>');
</script>
<style>
  body { padding-top: 100px; }
</style>
