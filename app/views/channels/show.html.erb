<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @channel.name %>
</p>

<div id="messages">
  <%= render @channel.messages %>
</div>

<%= render 'messages/form' %>

<%= subscribe_to "/channels/#{@channel.name}/messages/new" %>

<%= link_to 'Edit', edit_channel_path(@channel) %> |
<%= link_to 'Back', channels_path %>
